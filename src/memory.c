#include <assert.h>
#include <stdbool.h>
#include "memory.h"

static struct memory_context memory;
void initialize_memory(bool bootstrap_provided)
{
    // TODO
    assert(false);
}
BYTE memory_read(WORD address)
{
    // TODO
    assert(false);
}

void memory_write(WORD address, BYTE data)
{

    // TODO
    assert(false);
}